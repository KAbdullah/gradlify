/*
import React from 'react';
import { useNavigate } from 'react-router-dom';
import './student/FormStyles.css'; // Reuse existing styles

export default function HomePage() {
  const navigate = useNavigate();

  return (
    <div className="form-container">
      <h2 className="form-title">Welcome to Gradify</h2>
      <div className="form-group">
        <button className="submit-button" onClick={() => navigate('/professor')}>
          Professor
        </button>
      </div>
      <div className="form-group">
        <button className="submit-button" onClick={() => navigate('/student')}>
          Student
        </button>
      </div>
    </div>
  );
}
*/
